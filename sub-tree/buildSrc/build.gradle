apply plugin: 'groovy'
apply plugin: 'java-gradle-plugin'

gradlePlugin {
    plugins {
        ossPlugin {
            id = "oss-plugin"
            implementationClass = "OSSPlugin"
        }
    }
}